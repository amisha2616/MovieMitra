apiVersion: v1
kind: Service
metadata:
  name: moviemitra-service
spec:
  selector:
    app: moviemitra
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
